# AGENTS.md

本文件用于指导在本仓库工作的 AI 编程代理。开始修改前，先完整阅读 `README.md`；它是产品目标、学习方法和功能范围的主要事实来源。

## 1. 产品不可偏离的核心

这是“有效学习时间累计器”，不是普通日历打卡或习惯排行榜。

- 英语主目标：500 小时，训练项目为精听、泛听、阅读、复述、Anki。
- 日语副目标：800 小时，训练项目为影视精听、台词跟读、复述、泛听。
- 两种语言必须完全分账。日语分钟不得减少英语剩余时间，反之亦然。
- 倒计时只随已入账的有效学习分钟变化；自然时间流逝不能让用户“落后”。
- 产品文案应降低启动阻力，不使用断签惩罚、红色催促、欠账或失败叙事。
- 页面不仅记录时长，还要保留训练方案和能力里程碑，帮助用户执行正确的学习闭环。

如果功能便利性与这些原则冲突，以这些原则为准。

## 2. 开工前检查

1. 运行 `pwd`，确认工作目录是本项目。
2. 阅读 `README.md`、本文件、项目清单和现有测试。
3. 检查 `git status --short`，不要覆盖或回退用户已有修改。
4. 查找是否存在更深层的 `AGENTS.md`；修改某个目录时遵循离目标文件最近的说明。
5. 先理解现有数据结构和持久化键，再修改记录、统计或迁移逻辑。

## 3. 实现优先级

按以下顺序完成，前一层稳定后再增加下一层：

1. 双语言切换和页面基础布局。
2. 账号登录及云端记录的新增、读取、删除与持久化。
3. 语言独立的倒计时、统计和项目时间分布。
4. 始终可见的里程碑与能力说明。
5. 响应式布局、可访问性、错误状态和细节动效。
6. README 中列出的非 MVP 扩展。

不要为了扩展功能牺牲入账速度和首屏清晰度。

## 4. 数据模型与计算约束

学习记录至少应等价于：

```ts
type Language = "en" | "ja";

type StudyEntry = {
  id: string;
  date: string;       // 用户本地日期，格式 YYYY-MM-DD
  language: Language;
  activity: string;
  minutes: number;    // 正整数
  createdAt: string;  // ISO 时间或等价的稳定时间戳
};
```

约束：

- 新代码统一使用 `ja` 表示日语；若旧数据使用 `jp`，在读取层兼容并迁移，不能直接丢弃。
- ID 必须稳定且唯一；删除记录按 ID，不依赖可能碰撞的时间戳。
- 日期统计使用用户本地日期，不要用 `toISOString().slice(0, 10)` 直接替代本地日期，否则亚洲时区可能跨日错误。
- 所有汇总函数应显式接收语言或先按语言过滤，避免共享变量造成串账。
- 百分比封顶 100%，剩余时间下限为 0。
- 空数据时日均、预计完成日、项目占比不能出现 `NaN`、`Infinity` 或无意义日期。
- 月度热力图先按语言过滤，再按本地 `YYYY-MM` 和日期汇总；同一天多笔记录合并，使用固定强度区间，格子同时提供分钟数与无障碍标签。
- ETA 使用最近 28 个自然日的当前语言总分钟数除以 28，排除未来日期和窗口外记录；不能用“学习日日均”假设用户每天都会学习。
- 累计构成、每日节奏和月度热力图分别回答长期结构、逐日项目结构和月内频率，不应把三者合并成含义不清的单一图表。
- 里程碑列表是静态产品内容，即使无任何记录也必须渲染。

建议把计算逻辑写成无副作用的纯函数，并为边界条件提供单元测试。

## 5. 云端存储与本地迁移

- 当前版本为 online-only：必须登录并联网才能入账，不实现离线队列，也不能在网络失败时假装写入成功。
- Supabase 是账号认证和学习记录的事实来源；本地存储只用于读取旧版数据和保存“该账号已完成迁移”的幂等标记。
- 前端只允许使用 publishable/anon key。数据库密码、secret key、service-role key 等高权限凭据不得进入浏览器、Git 或 Vercel 环境变量。
- `study_entries` 必须启用 RLS，所有查询和写入都要受 `auth.uid() = user_id` 约束；不能仅依赖前端过滤实现账号隔离。
- `milestone_checks` 同样必须启用 RLS，以 `(user_id, language, milestone_hours)` 为唯一键；到达小时节点只是前端操作门槛，保存的是用户主动确认的真实验收结果。
- 不更改既有本地存储键或数据格式，除非同时提供幂等迁移。
- 已知旧键包括 `en500h_v1` 和 `lang_countdown_v2`。
- 迁移必须满足：只运行一次、可重复加载、不会重复导入、不会把旧英语记录误判为日语。
- 任何 schema 升级都应通过 `supabase/migrations/` 留下可审计迁移，并兼容已有云端记录。
- 清空数据是破坏性操作，必须二次确认；不要把“重置”放在容易误触的位置。
- 首次登录自动上传有效旧记录，使用 `(user_id, client_ref)` 唯一约束避免重复导入；上传成功后保留旧本地副本。
- 若增加导入导出，先校验文件结构，再合并数据，并为重复记录给出明确策略。
- JSON / CSV 导出只能包含当前账号已加载的学习记录和验收状态，不能包含 access token、密码、数据库凭据或本地迁移标记。

## 6. UI 与交互规范

- 中文为默认界面语言。
- 首屏顺序优先为：语言页签 → 剩余小时 → 进度 → 快速入账。
- 英语和日语布局结构一致，不能只修好当前可见页。
- 页签使用语义正确的按钮或 ARIA tab 模式，支持键盘和清晰焦点态。
- 表单必须有可见标签，不能只依赖 placeholder。
- 错误反馈使用行内文字，不能用静默失败；成功反馈应简短且不阻塞下一笔输入。
- 无数据时仍展示完整页面结构，并提供温和的行动提示。
- 统计、里程碑和最近记录不得依赖某一块成功后才渲染；单个模块出错不应让整页空白。
- 颜色不能是唯一状态信号。里程碑完成状态同时使用图标或文字。
- 支持窄屏、触摸操作、系统字体缩放和 `prefers-reduced-motion`。
- 保持视觉安静、可信、长期耐看；不要加入排行榜、徽章轰炸、强制连续打卡或焦虑式通知。

## 7. 内容与术语

固定术语：

- “入账”：记录一次有效学习时间。
- “剩余小时”：距离该语言目标尚需累计的时间。
- “学习天数”：存在至少一条该语言记录的去重日期数。
- “连续天数”：辅助统计，不是失败判定。
- “里程碑验收”：到达小时阈值后应尝试的真实能力测试。

不要擅自把 500 小时或 800 小时描述为科学保证。它们是基于当前水平和目标设定的可执行估算，页面应避免承诺“投入即必然流利”。

## 8. 测试要求

每次涉及功能改动，至少验证：

### 记录和统计

- 英语新增记录只改变英语总量、分布、学习天数和 ETA。
- 日语新增记录只改变日语对应数据。
- 同一天多条记录只增加一个学习天数，但分钟数全部累加。
- 删除一条记录后所有衍生统计同步回退。
- 输入为空、0、负数、小数、超大值和非数字时行为明确。
- 目标刚好完成、超过目标和无数据三种边界状态正确。

### 日期

- 当天有记录时连续天数包含今天。
- 当天没有但昨天有记录时，从昨天的连续段计算。
- 跨月、跨年和夏令时环境下不发生日期偏移。

### 渲染和交互

- 首次打开时两个页签、全部里程碑、空记录提示均存在。
- 页签鼠标点击、触摸和键盘切换都可用。
- 刷新后数据仍在，迁移数据不会重复。
- 删除、重置的确认与取消路径均可用。
- 手机宽度下没有横向溢出。
- 当月首日星期偏移、当月天数、未来日期弱化和热力等级均正确；切换语言时热力图不串账。
- 7 / 14 / 30 天每日节奏图的总量、项目堆叠、空白日期和移动端横向滚动正确。
- 28 日 ETA 跨月跨年窗口正确，排除未来记录，目标完成或近 28 日无记录时状态正确。
- 里程碑验收新增、刷新、取消均云端同步；未到达节点不能操作。
- 月份/项目筛选、JSON/CSV 导出、忘记密码和修改密码的成功与错误路径可用。
- 浏览器控制台无 JavaScript 语法错误、未捕获异常和 hydration 错误。

优先写自动化测试覆盖纯计算、存储迁移和表单校验。完成 UI 修改后还要在真实浏览器中走一遍“切换语言 → 入账 → 刷新 → 删除”的完整流程。

## 9. 工程约定

- 当前技术栈是静态 HTML/CSS/JavaScript + Supabase Auth/Postgres，部署在 Vercel；把启动、部署和测试命令同步到 `README.md`。
- 不要在没有明确收益时引入状态管理框架或额外后端服务。
- 将目标、活动、里程碑定义集中配置，避免在不同组件复制后发生不一致。
- 将领域计算、持久化和展示层分离；纯计算放在 `domain.js` 并由 `tests/domain.test.js` 覆盖，不要把全部业务逻辑塞进一个组件或内联脚本。
- 使用明确类型或运行时校验保护本地存储中的旧数据和损坏数据。
- 修改文件使用小而聚焦的补丁，保留与任务无关的用户改动。
- 不提交密钥、个人学习记录、构建产物或本机专用绝对路径。
- 新增依赖前说明用途；优先复用项目已有能力。

## 10. 完成定义

一个任务只有在以下条件满足时才算完成：

- 实现符合 `README.md` 中相关需求和产品原则。
- 已检查英语与日语两个页面，没有串账或一侧失效。
- 相关测试通过，并执行了与改动风险相称的浏览器验证。
- 无新增控制台错误、明显可访问性退化或移动端布局问题。
- 数据格式变化附有迁移和回归测试。
- README、类型、测试或注释已同步更新，使下一位代理不需要重新猜测决策背景。

发现 README 与代码不一致时，不要静默选择一方：先以用户最近的明确要求为准，再同步修正文档与实现。
